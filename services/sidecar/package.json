{
  "name": "argus-sidecar",
  "version": "1.0.0",
  "description": "TikTok URL signing sidecar (X-Bogus + msToken)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
