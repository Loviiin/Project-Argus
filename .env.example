# Sample environment for local dev (do not commit .env with secrets)

# Postgres (used by docker-compose.yml)
POSTGRES_USER=argus-user
POSTGRES_PASSWORD=change_me
POSTGRES_DB=argus-post-db

# Application
DATABASE_URL=postgres://argus-user:change_me@127.0.0.1:5432/argus-post-db?sslmode=disable
NATS_URL=nats://localhost:4222
DISCORD_TOKEN=
